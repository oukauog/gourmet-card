# グルメカード — 実装役への常設ルール

あなたは「実装役」です。仕様は `docs/01_仕様書.md` が正です。依頼文に書かれた工事だけを行い、範囲外の変更はしないでください。迷ったら実装せずに報告書に質問として書いてください。

## 1. プロダクトの前提
- スマホ（iPhone中心、Android可）で使うPWA。店データ・写真は端末内（IndexedDB）のみ。サーバーは作らない
- 必須項目は店名だけ。他の項目を必須にする変更は禁止
- 見た目の華やかさは要件。画面は必ずスマホ幅（390×844目安）で確認する
- 公開先は GitHub Pages（公開リポジトリ）。個人情報・鍵類をリポジトリに入れない

## 2. コードを壊さない
- **B-1** テキスト置換型の編集で既存コードを直接書き換えない。編集はスクリプト（Python/Node）経由の決定的な手段で行い、編集後に型チェック・ビルド・テストまで同じ流れで実行する
- **B-2** 工事に着手する前に `git status` がクリーンであることを確認し、工事ごとにブランチを切る（例: `work/00-setup`）。動いた状態ごとにコミットする。クリーンでなければ作業を始めず報告する
- **B-3** 新機能は新規ファイル＋既存からの呼び出しで足す。既存ファイルの改修は最小に

## 3. 品質と検収
- **C-1** 既存のテストが全部通ること。新機能をOFFにした状態で、改修前と同じ入力に対して同じ出力になること。テストの無い箇所を触る前に、現状の挙動を固定するテストを書く
- **C-2** 直したバグは恒久テストで固定する
- **C-3** 「実装完了」と書いてよいのは、利用者がスマホの画面から使える状態になった時だけ。それ以前は「機能完成（組込待ち）」と書く
- **C-4** テストが通っても完成とは限らない。最終判定は人間が実機で行う
- **C-5** 複数ファイルにまたがる工事・挙動が変わる工事では、検証レポートを書く（何をやったか／何を確認したか／何を保留したか）
- 書き出し→取り込みの往復一致テストは絶対に壊さない（利用者のバックアップの生命線）

## 4. 日本語Windows環境での注意
- バッチファイル（.bat / .cmd）を作らない・使わない。スクリプトは npm scripts / Node / Python で書く
- コンソールに出す文字は文字化けを避ける（ログ・進捗表示は英数字中心。ファイルは UTF-8（BOMなし）で読み書きし、エンコーディングを明示する）
- パスに日本語や空白を含む前提で扱う（クォートする）

## 5. 報告
- 工事の終わりに `reports/工事NN_報告.md` を書く（相談役が直接読む）。NN は2桁（工事0 → `工事00_報告.md`）
- 必須項目：やったこと／変更ファイル一覧／テスト結果（件数・合否）／ブランチ名とコミット／花井さんが実機で確認すべき手順／保留・質問
- 報告の中で「実装完了」「機能完成（組込待ち）」を区別する

# グルメカード 仕様書（v0.2）

- 作成: 2026-09-24 相談役（Cowork）／v0.2 更新: 2026-09-24（8章 確認事項1・2・5 回答反映）
- 状態: 工事0（環境構築）着手
- 位置づけ: 生きた引継ぎ文書（ルールA-2）。決定が変わったら必ずここを更新する
- 関連: 00_開発ルール（相談役・実装役体制）.md ／ CLAUDE.md

---

## 1. 目的

自分が行った良い飲食店を「切れるカード（手札）」として貯め、聞かれたらその場でさっと見せる。友人に「富山でうまい所ない？」と聞かれた時、5秒で答えられることが最重要の評価軸。

裏の目的：お気に入り店の料理写真がずらっと並ぶコレクションとして、眺めて楽しめること。**見た目の華やかさは機能と同格の要件**。

### 1.1 作らないもの（スコープ外）
- 常時同期・フォロー等のSNS機能
- アカウント・ログイン
- サーバー上へのデータ保存（店データ・写真は各自の端末内のみ）
- 訪問日・訪問回数の記録
- 予算感の記録

### 1.2 後回し（将来の拡張候補。今は作らないがデータ形式で邪魔しない）
- GoogleマップURLからの店名・県・市の自動入力（短縮URL展開に小サーバーが要るため）
- コード発行によるお裾分け（方式2、小サーバー）
- メモの秘匿配慮（見せる時の折りたたみ、送信時の除外）

---

## 2. 利用環境と構成

| 項目 | 内容 |
|---|---|
| 主な端末 | スマホ（iPhone多め、Androidも同等に動くこと） |
| 副 | PC（ブラウザで入力・閲覧できれば可） |
| 形態 | PWA（Webアプリをホーム画面に追加して使う） |
| データ保存 | 端末内（IndexedDB）。サーバーなし |
| 公開 | **GitHub Pages**（アプリ本体の静的ファイルのみ。2026-09-24 決定） |
| 開発PC | 日本語Windows、ユーザー名 `owner` |
| フォルダ | `C:\Users\owner\Documents\Claude\Projects\gourmet-card\`（確定） |
| リポジトリ | GitHub **公開リポジトリ**（GitHub Pages 無料利用の条件） |

### 2.1 技術スタック（相談役の推奨。工事0で実装役に最終確認させる）
- Vite ＋ TypeScript ＋ React
- IndexedDB ラッパー: Dexie.js
- PWA化: vite-plugin-pwa
- 書き出しファイル: ZIP（JSON＋画像）。ライブラリは JSZip 等
- テスト: Vitest（ロジック）、Playwright（画面の通し確認、スマホ幅で）
- 選定理由: 情報量が多く実装役が迷いにくい定番構成。静的ファイルだけで動くので無料ホスティングに載る

### 2.2 公開先（決定：GitHub Pages）
- 無料プランでは公開リポジトリが必要。ソースコードは公開されるが、店データ・写真はリポジトリに含まれないため実害は小さい、と判断して採用
- 公開URLは `https://<ユーザー名>.github.io/gourmet-card/` の形になる見込み。Vite の `base` 設定をこれに合わせる（工事9）
- 個人情報・APIキー等をリポジトリに入れないこと（現状そのようなものは無い）

### 2.3 端末内保存の性質（利用者に周知する事項）
- 写真は登録時にアプリ内へ**コピー**する。カメラロールから消してもアプリ側は消えない
- 次の場合はアプリ内データが消える：アプリをホーム画面から削除／Safariの「Webサイトデータを消去」／機種変更
- 対策：起動時に `navigator.storage.persist()` を要求（WebKitはホーム画面アプリだと許可しやすい方針）＋ **全店書き出し（バックアップ）を必須機能**とし、最終バックアップから一定日数で促す

---

## 3. データ設計

### 3.1 大原則
**必須は店名だけ。他はすべて任意。** 全部入れないと登録できない作りは不可（入力の手間が際限なく増えて使われなくなるため）。

### 3.2 店（Shop）
| 項目 | 型・内容 | 必須 | 備考 |
|---|---|---|---|
| id | 一意ID（UUID） | 自動 | お裾分け時の重複判定にも使う |
| name | 店名（文字列） | **必須** | |
| status | `visited`（手札）／`wishlist`（行きたい） | 自動 | 自分登録の初期値は visited、登録時に切替可 |
| photos | 写真ID配列（最大3、先頭が表紙） | 任意 | 並べ替えで表紙を変更可 |
| rating | 0.1〜5.0（0.1刻み）または未設定 | 任意 | 内部は整数（1〜50）で持つと誤差が出ない |
| prefecture | 都道府県（選択式） | 任意 | |
| city | 市区町村（選択式） | 任意 | 県選択後に候補表示 |
| stationId | 駅ID | 任意 | 駅マスタ参照 |
| areaTags | エリアタグID配列 | 任意 | 自由追加（例：総曲輪、八尾、国道8号沿い） |
| genreTags | ジャンルタグID配列 | 任意 | 自由追加（例：ラーメン、海鮮） |
| useTags | 使い道タグID配列 | 任意 | 用意リストから選択（例：個室あり） |
| mapUrl | GoogleマップURL | 任意 | タップで開くだけ。解析はしない |
| memo | 文字列 | 任意 | 常に表示・送信 |
| origin | `self`／`shared` | 自動 | お裾分けで取り込んだ店は shared |
| createdAt / updatedAt | 日時 | 自動 | 並び順（登録順）に使う |

### 3.3 写真（Photo）
- 1枚につき2サイズを保存
  - 一覧用（小）：長辺 約400px、JPEG
  - 店ページ用（大）：長辺 約1600〜2000px、JPEG 品質は高め（がびがびにしない。目安 0.85）
- 元画像は保存しない（元の高画質はカメラロールに残る前提）
- 撮影向き（EXIF）を正しく反映すること。iPhoneのHEICも取り込めること（要実機確認）

### 3.4 タグ（Tag）
- 種別：`area`／`genre`／`use`
- 表記ゆれ対策（genre・area）
  1. 入力中に既存タグを候補表示（新規作成より選ぶ方が楽な画面にする）
  2. 全角/半角・大文字/小文字の違いは同一とみなす（正規化キーで一致判定）
  3. タグ整理画面：名前変更・統合（統合すると付いていた店は新しい方へ付け替え）
- use タグは初期リストを同梱（中身は工事時に花井さんと決める。当面「個室あり」のみ確定）。後から追加・編集可

### 3.5 駅マスタ
- 全国の駅一覧をアプリに同梱（オフラインで候補表示できるように）
- 表示名：同名駅がある場合のみ「府中（東京都）」のように県名を併記
- 候補の優先：県が選択済みならその県の駅を優先
- 駅選択時、県・市が空なら駅の所在地で自動補完（上書きはしない）
- データ候補：国土数値情報（鉄道）※2020年以降版はオープンデータ規約。駅名・路線・事業者を含むが県市は別途付与が要る見込み。代替：駅データ.jp 等。**工事7の前に利用条件と出典表記を相談役が確定**

### 3.6 設定（Settings）
- 最終バックアップ日時、並び順・列数などの表示設定

---

## 4. 画面仕様

### 4.1 一覧画面（ホーム）
- 上部：切替タブ「手札」「行きたい」（初期は手札）
- 上部：絞り込みバー — 場所（県・市・駅・エリア）／ジャンル／使い道／評価（例：4.0以上）／未評価のみ
- 並び替え：登録の新しい順（初期）／評価の高い順／名前順
- 本体：写真タイルのグリッド。タイル＝表紙写真（一覧用小）＋店名＋☆
  - 写真のない店：店名だけのシンプルなタイル（見栄えを崩さないデザイン）
  - 列数（2列/3列）は**見本を見比べて決定**（8章 確認事項3）
- 右下：「＋」（新規登録）
- 目標：スクロールが引っかからないこと（一覧用小画像のみ読み込む）

### 4.2 登録・編集画面
- 目標：**写真＋店名だけなら10秒で保存**
- 上部：写真（撮影 or アルバムから選択、最大3枚、並べ替えで表紙変更）
- 店名（必須）→ 保存ボタンは常に見える位置に
- その下に任意項目（評価、場所、タグ、URL、メモ、手札/行きたい）
- 評価入力：0.1刻みで入れやすいUI（スライダー＋数値微調整など。見本で決める）
- 保存後は店ページへ

### 4.3 店ページ
上から順に：
1. 写真（画面幅いっぱい、最大3枚を横スワイプ）
2. 店名
3. ☆（比例塗り）＋数値「3.7」
4. タグ（ジャンル・使い道）
5. 場所（県・市／最寄り駅／エリア）
6. 「Googleマップで開く」ボタン（URLがある時のみ）
7. メモ
8. 「編集」「この店を送る」

- 一画面目は写真・店名・評価で埋まり、下にスクロールで詳細、を目安にする
- 「行きたい」の店には「行った！」ボタン → 押すと status を visited にし、編集画面を開いて写真・評価の追加を促す（昇格）

### 4.4 ☆表示ルール
- ☆5個。評価値をそのまま塗り量にする（3.7 → ☆3個全塗り＋4個目を70%塗り）
- 数値を必ず併記（小数1桁）
- 未評価は「未評価」と表示（☆0個と区別する）

### 4.5 お裾分け・バックアップ画面
- 書き出し：1店（店ページから）／複数選択（一覧で選択モード）／全店（バックアップ）
- 共有：Web Share API でファイルを共有メニューに渡す（LINE・AirDrop・メール等）。非対応環境ではダウンロード
- 取り込み：「取り込む」→ファイル選択
  - 他人から来た店は origin=shared、status=wishlist で取り込む（自分のバックアップ復元時は元の状態のまま）
  - 同じidの店が既にある場合：スキップ／上書きを選択（初期はスキップ）
  - 相手のタグはそのまま取り込み（正規化キーが一致すれば既存タグに合流）。揺れは整理画面で統合
- バックアップの促し：最終書き出しから30日（仮）で一覧画面に控えめな案内

---

## 5. 書き出しファイル形式
- 拡張子：`.gcard`（中身はZIP）※拡張子は要検討（iOSで扱いやすい方を工事8で実機確認。ダメなら `.zip`）
- 中身：`manifest.json`（形式バージョン、書き出し日時、種別=share/backup）、`shops.json`、`tags.json`、`photos/` 以下に画像
- **形式バージョンを必ず持つ**（将来の方式2や項目追加で読み替えできるように）
- 方式2（コード発行）を足す時も同じ形式を使う

---

## 6. 品質基準（ルールC）
- ロジック（評価の丸め・☆塗り量、タグ正規化、絞り込み、書き出し⇔取り込みの往復一致、写真縮小サイズ）はVitestで恒久テスト
- 書き出し→取り込みで**全項目が一致する往復テスト**は最重要（バックアップの生命線）
- 画面はPlaywrightでスマホ幅（390×844 目安）の通し確認
- 「実装完了」＝スマホのブラウザで花井さんが実際に使える状態（C-3）。それ以前は「機能完成（組込待ち）」
- 最終判定は花井さんの実機確認（iPhone必須、Androidは可能なら）

---

## 7. 工事計画（案）
一度にひとつ。各工事の前に相談役が依頼文を書く。

| 工事 | 内容 | 検収の目安 |
|---|---|---|
| 0 | 環境構築：Git初期化、Vite雛形、CLAUDE.md・仕様書の配置確認、テスト基盤、`npm run dev` でPC表示、スマホから同一LANで表示 | 空画面がスマホで見える、テストが走る |
| 1 | データ層：Dexieスキーマ（店・写真・タグ・設定）、CRUD、タグ正規化 | ユニットテスト |
| 2 | 最小登録：店名＋写真（2サイズ縮小保存） | スマホで登録できる |
| 3 | 一覧タイル（見本2列/3列を作って比較→決定） | 花井さんが見た目判定 |
| 4 | 店ページ（写真スワイプ、☆比例塗り） | 実機で表示確認 |
| 5 | 任意項目の入力：評価・県市・タグ・URL・メモ・手札/行きたい | |
| 6 | 絞り込み・並び替え・手札/行きたい切替・未評価のみ | |
| 7 | 駅マスタ同梱と候補表示 | |
| 8 | 書き出し・取り込み・共有（往復テスト必須、検証レポート） | iPhone⇔iPhoneで実際に送受信 |
| 9 | PWA化・GitHub Pages公開・persist要求・バックアップ促し | ホーム画面追加して使える＝実装完了 |
| 10 | 見た目の仕上げ・タグ整理画面 | |

- 工事3以降、見た目に関わる工事は「見本を複数出させて花井さんが選ぶ」を基本にする
- C-5 検証レポート対象：工事1、2、8、9（目安）
- 報告は実装役がプロジェクトフォルダの `reports/` に `工事NN_報告.md` で保存し、相談役が直接読む

---

## 8. 確認事項（花井さんへ）
1. ~~フォルダのユーザー名~~ → **回答済み（2026-09-24）**：`owner` のPCで開発（報告書ツールとは別PC）
2. ~~公開先~~ → **回答済み（2026-09-24）**：GitHub Pages。公開リポジトリでよい
3. **タイルの列数**：工事3で見本を比較して決定
4. **使い道タグの初期リスト**：工事5までに一緒に決める（確定は「個室あり」のみ）
5. ~~起動コマンド~~ → **回答済み（2026-09-24）**：報告書ツールと同じ `claude --dangerously-skip-permissions`

---

## 9. 決定の経緯（要点）
- 評価は当初0.1〜10案 → ☆10個になるため0.1〜5.0・☆5個に変更
- GoogleマップURL自動入力は、短縮URL展開と住所取得の手間から「できたら」に格下げ
- 写真の店ページ用サイズは当初長辺1000px案 → がびがび回避のため1600〜2000pxに修正（インスタは横1080px程度とされる）
- お裾分けはサーバーなしのファイル方式から開始。手間が気になれば方式2を追加
- メモの秘匿配慮は「ひとまず」なし（見られて困ることは書かない前提）
- 公開先は GitHub Pages（公開リポジトリ）。店データ・写真はリポジトリに入らないためソース公開の実害は小さいと判断
- 詳細はプロジェクトメモリ hearing-decisions を参照

## 変更履歴
- v0.1 2026-09-24 初版
- v0.2 2026-09-24 確認事項1・2・5の回答を反映（開発PC=owner、公開先=GitHub Pages、起動コマンド確定）。工事0の内容にGit初期化・仕様書配置確認を明記
